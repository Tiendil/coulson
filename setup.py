# to install from sources in old projects

import setuptools


setuptools.setup(
    name='coulson',
    version='0.1',
    description='Control types of variables in runtime',
    url='https://github.com/Tiendil/coulson',
    author='Aliaksei Yaletski (Tiendil)',
    author_email='a.eletsky@gmail.com',
    license='BSD',
    packages=setuptools.find_packages(),
    install_requires=[])
